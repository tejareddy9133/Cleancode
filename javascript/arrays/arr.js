let arr = [1, 23, 45, 67, 34];

//access

arr.pop(); //deleting last element
arr.push(10000); // we can add elements in to array

arr.shift(); //deleting the first element
arr.unshift(9000); //add the first element to the array

console.log(arr.indexOf(23)); //index
