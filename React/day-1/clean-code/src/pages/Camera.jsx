import React from "react";
import Sidebar from "../components/Sidebar";

const Camera = () => {
  let Naam = "Camera";
  return (
    <div>
      <Sidebar name={Naam} />
    </div>
  );
};

export default Camera;
